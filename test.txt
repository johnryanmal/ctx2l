expr:
| V=sum -> V

sum:
| L=prod '+' R=prod -> add(L, R)
| L=prod '-' R=prod -> sub(L, R)

prod:
| L=pow '*' R=pow -> mul(L, R)

pow:
| L=value '**' R=value -> pow(L, R)

value:
| '(' V=expr ')' -> V
| V=DIGITS -> V

DIGITS:
| [0-9]+
